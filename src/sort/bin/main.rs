fn main() {
    let A = vec![5, 2, 4, 6, 1, 3];
    let mut sorted: Vec<i32> = vec![];

    let len = &A.len();
    for j in 1..*len {
        key = A[j];
        let i = j - 1;

        while i > 0 && A[i] > key {

        }
    }
}
